// Attache un écouteur d'événement pour détecter les touches du clavier
document.addEventListener('keydown', function(event) {
    // Vérifie si la touche appuyée est la touche souhaitée
    if (event.key === 't') {
        // Empêche le comportement par défaut de la touche
        event.preventDefault();

        // Appelle la fonction pour faire défiler vers l'élément avec l'ID spécifié, par exemple 'monElement'
        scrollToElement('top');
    }
});

document.addEventListener('keydown', function(event) {
    // Vérifie si la touche appuyée est la touche souhaitée
    if (event.key === '1') {
        // Empêche le comportement par défaut de la touche
        event.preventDefault();

        // Appelle la fonction pour faire défiler vers l'élément avec l'ID spécifié, par exemple 'monElement'
        scrollToElement('Season1');
    }
});

document.addEventListener('keydown', function(event) {
    // Vérifie si la touche appuyée est la touche souhaitée
    if (event.key === '&') {
        // Empêche le comportement par défaut de la touche
        event.preventDefault();

        // Appelle la fonction pour faire défiler vers l'élément avec l'ID spécifié, par exemple 'monElement'
        scrollToElement('Season1');
    }
});

document.addEventListener('keydown', function(event) {
    // Vérifie si la touche appuyée est la touche souhaitée
    if (event.key === '2') {
        // Empêche le comportement par défaut de la touche
        event.preventDefault();

        // Appelle la fonction pour faire défiler vers l'élément avec l'ID spécifié, par exemple 'monElement'
        scrollToElement('Season2');
    }
});

document.addEventListener('keydown', function(event) {
    // Vérifie si la touche appuyée est la touche souhaitée
    if (event.key === 'é') {
        // Empêche le comportement par défaut de la touche
        event.preventDefault();

        // Appelle la fonction pour faire défiler vers l'élément avec l'ID spécifié, par exemple 'monElement'
        scrollToElement('Season2');
    }
});

document.addEventListener('keydown', function(event) {
    // Vérifie si la touche appuyée est la touche souhaitée
    if (event.key === '3') {
        // Empêche le comportement par défaut de la touche
        event.preventDefault();

        // Appelle la fonction pour faire défiler vers l'élément avec l'ID spécifié, par exemple 'monElement'
        scrollToElement('Season3');
    }
});

document.addEventListener('keydown', function(event) {
    // Vérifie si la touche appuyée est la touche souhaitée
    if (event.key === '"') {
        // Empêche le comportement par défaut de la touche
        event.preventDefault();

        // Appelle la fonction pour faire défiler vers l'élément avec l'ID spécifié, par exemple 'monElement'
        scrollToElement('Season3');
    }
});

document.addEventListener('keydown', function(event) {
    // Vérifie si la touche appuyée est la touche souhaitée
    if (event.key === '4') {
        // Empêche le comportement par défaut de la touche
        event.preventDefault();

        // Appelle la fonction pour faire défiler vers l'élément avec l'ID spécifié, par exemple 'monElement'
        scrollToElement('Season4');
    }
});

document.addEventListener('keydown', function(event) {
    // Vérifie si la touche appuyée est la touche souhaitée
    if (event.key === '5') {
        // Empêche le comportement par défaut de la touche
        event.preventDefault();

        // Appelle la fonction pour faire défiler vers l'élément avec l'ID spécifié, par exemple 'monElement'
        scrollToElement('Season5');
    }
});

document.addEventListener('keydown', function(event) {
    // Vérifie si la touche appuyée est la touche souhaitée
    if (event.key === '(') {
        // Empêche le comportement par défaut de la touche
        event.preventDefault();

        // Appelle la fonction pour faire défiler vers l'élément avec l'ID spécifié, par exemple 'monElement'
        scrollToElement('Season5');
    }
});

document.addEventListener('keydown', function(event) {
    // Vérifie si la touche appuyée est la touche souhaitée
    if (event.key === '6') {
        // Empêche le comportement par défaut de la touche
        event.preventDefault();

        // Appelle la fonction pour faire défiler vers l'élément avec l'ID spécifié, par exemple 'monElement'
        scrollToElement('Season6');
    }
});

document.addEventListener('keydown', function(event) {
    // Vérifie si la touche appuyée est la touche souhaitée
    if (event.key === '-') {
        // Empêche le comportement par défaut de la touche
        event.preventDefault();

        // Appelle la fonction pour faire défiler vers l'élément avec l'ID spécifié, par exemple 'monElement'
        scrollToElement('Season6');
    }
});

document.addEventListener('keydown', function(event) {
    // Vérifie si la touche appuyée est la touche souhaitée
    if (event.key === '7') {
        // Empêche le comportement par défaut de la touche
        event.preventDefault();

        // Appelle la fonction pour faire défiler vers l'élément avec l'ID spécifié, par exemple 'monElement'
        scrollToElement('Season7');
    }
});

document.addEventListener('keydown', function(event) {
    // Vérifie si la touche appuyée est la touche souhaitée
    if (event.key === 'è') {
        // Empêche le comportement par défaut de la touche
        event.preventDefault();

        // Appelle la fonction pour faire défiler vers l'élément avec l'ID spécifié, par exemple 'monElement'
        scrollToElement('Season7');
    }
});

document.addEventListener('keydown', function(event) {
    // Vérifie si la touche appuyée est la touche souhaitée
    if (event.key === '8') {
        // Empêche le comportement par défaut de la touche
        event.preventDefault();

        // Appelle la fonction pour faire défiler vers l'élément avec l'ID spécifié, par exemple 'monElement'
        scrollToElement('Season8');
    }
});

document.addEventListener('keydown', function(event) {
    // Vérifie si la touche appuyée est la touche souhaitée
    if (event.key === '_') {
        // Empêche le comportement par défaut de la touche
        event.preventDefault();

        // Appelle la fonction pour faire défiler vers l'élément avec l'ID spécifié, par exemple 'monElement'
        scrollToElement('Season8');
    }
});

document.addEventListener('keydown', function(event) {
    // Vérifie si la touche appuyée est la touche souhaitée
    if (event.key === '9') {
        // Empêche le comportement par défaut de la touche
        event.preventDefault();

        // Appelle la fonction pour faire défiler vers l'élément avec l'ID spécifié, par exemple 'monElement'
        scrollToElement('Season9');
    }
});

document.addEventListener('keydown', function(event) {
    // Vérifie si la touche appuyée est la touche souhaitée
    if (event.key === 'ç') {
        // Empêche le comportement par défaut de la touche
        event.preventDefault();

        // Appelle la fonction pour faire défiler vers l'élément avec l'ID spécifié, par exemple 'monElement'
        scrollToElement('Season9');
    }
});


// Fonction pour faire défiler vers un élément par son ID
function scrollToElement(elementId) {
    // Récupère l'élément par son ID
    var element = document.getElementById(elementId);

    // Vérifie si l'élément existe
    if (element) {
        // Utilise la méthode scrollIntoView pour faire défiler la page jusqu'à l'élément
        element.scrollIntoView({ behavior: 'smooth' });
    }
}
